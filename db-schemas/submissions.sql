"CREATE TABLE submissions(cik VARCHAR, entityType VARCHAR, sic VARCHAR, sicDescription VARCHAR, ownerOrg VARCHAR, insiderTransactionForOwnerExists BIGINT, insiderTransactionForIssuerExists BIGINT, ""name"" VARCHAR, tickers VARCHAR[], exchanges VARCHAR[], ein VARCHAR, lei JSON, description VARCHAR, website VARCHAR, investorWebsite VARCHAR, category VARCHAR, fiscalYearEnd VARCHAR, stateOfIncorporation VARCHAR, stateOfIncorporationDescription VARCHAR, addresses MAP(VARCHAR, STRUCT(street1 VARCHAR, street2 JSON, city VARCHAR, stateOrCountry VARCHAR, zipCode VARCHAR, stateOrCountryDescription VARCHAR, isForeignLocation BIGINT, foreignStateTerritory JSON, country JSON, countryCode JSON)), phone VARCHAR, flags VARCHAR, formerNames MAP(VARCHAR, VARCHAR)[], filings STRUCT(recent STRUCT(accessionNumber VARCHAR[], filingDate DATE[], reportDate VARCHAR[], acceptanceDateTime VARCHAR[], act VARCHAR[], form VARCHAR[], fileNumber VARCHAR[], filmNumber VARCHAR[], items VARCHAR[], core_type VARCHAR[], size BIGINT[], isXBRL BIGINT[], isInlineXBRL BIGINT[], primaryDocument VARCHAR[], primaryDocDescription VARCHAR[]), files STRUCT(""name"" VARCHAR, filingCount BIGINT, filingFrom DATE, filingTo DATE)[]));"
